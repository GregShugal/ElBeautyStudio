---
const { beforeSrc = '/assets/placeholder-before.jpg', afterSrc = '/assets/placeholder-after.jpg', caption = '' } = Astro.props;
---
<div class="card">
  <div class="grid grid-cols-2 gap-2">
    <img src={beforeSrc} alt="Before" class="rounded-xl w-full h-full object-cover" loading="lazy" />
    <img src={afterSrc} alt="After" class="rounded-xl w-full h-full object-cover" loading="lazy" />
  </div>
  {caption && <p class="mt-3 text-sm text-[var(--muted)]">{caption}</p>}
</div>
